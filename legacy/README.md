# Legacy (pre-Strata CoverFi) pages — archived, not served

These are the original CoverFi multi-page dApp screens. They are **not part of the
Strata app** (which lives in `frontend/` and is navigated via `strata-shell.js`), are
**not deployed** (Vercel builds `frontend/**` only), and are kept here purely for
git/history reference.

They were quarantined out of `frontend/` because they predate the Mantle migration
(HashKey / Chain-133 era) and the CoverFi→Strata rebrand. Do not link to them from the
Strata app.

Moved here: `dashboard.html`, `stats.html`, `subrogation.html`, `coverage.html`,
`register.html`, `attestor.html`, `issuer-dashboard.html`, `index.html.bak`.
